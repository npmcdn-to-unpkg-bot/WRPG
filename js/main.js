// Eventually the main controller. For now a bunch of console logging.

//Character(id, name, race, level, maxHealth, maxEnergy, maxMana, strength, agility, intelligence, charisma, xpos, ypos)

var test = new Character(1, "Rupert", "Human", 1, 100, 50, 25, 9, 7, 4, 5, 0, 0);

console.log(test.getName());
console.log(test.getHealth());

console.log(test.getStrength());
